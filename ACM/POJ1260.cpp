#include <stdio.h>
#include<stdlib.h>
#include<string.h>
int main (int argc, const char * argv[]) {
	int cases,n,i,j;
	int price[1010];
	scanf("%d",&cases);
	while (cases--) {
		scanf("%d",&n);
	}

    return 0;
}
