#include <stdio.h>
#include<stdlib.h>
#include<string.h>

int main (int argc, const char * argv[]) {
	int a,b,n;
	int i,j;
	while (1) {
		scanf("%d %d %d",&a,&b,&n);
		if (a==0&&b==0&&c==0) 
			break;
		int table[8][8];
		
	}
    return 0;
}
